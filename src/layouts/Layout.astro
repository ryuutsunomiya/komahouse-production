---
import 'destyle.css';
import '../styles/global.sass';
import '../styles/components.sass';
import '../styles/pages.sass';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

interface Props {
	title?: string;
	description?: string;
	ogType?: 'website' | 'article';
}

const { title, description: pageDescription, ogType = 'website', } = Astro.props;

const baseDescription = "";
const description = pageDescription ? pageDescription : baseDescription;

const baseTitle = 'KOMA HOUSE';
const pageTitle = title ? `${title} - ${baseTitle}` : baseTitle;

const pageUrl = Astro.url.href;
const ogpUrl = new URL('/ogp.png', Astro.url.origin).href;
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>{pageTitle}</title>
    <meta name="description" content={description}/>

    <meta property="og:title" content={pageTitle} />
    <meta property="og:type" content={ogType} />
    <meta property="og:url" content={pageUrl} />
    <meta property="og:description" content={description} />
    <meta property="og:site_name" content={baseTitle} />
	<meta property="og:locale" content={"ja_JP"} />
    <meta property="og:image" content={ogpUrl} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content={pageTitle} />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={pageTitle} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogpUrl} />
    <link rel="icon" href="/Favicon.png" />

    <meta name="generator" content={Astro.generator} />

	<link rel="preconnect" href="https://fonts.googleapis.com">
	<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
	<link href="https://fonts.googleapis.com/css2?family=Zen+Kaku+Gothic+New:wght@400;500&family=Zen+Old+Mincho:wght@500&display=swap" rel="stylesheet">
	<link rel="stylesheet" href="https://use.typekit.net/nyw6mhh.css">

    <meta name="robots" content="noindex, nofollow" />
  </head>
	<body>
		<Header />
		<main id="pageWrapper">
			<slot />
		</main>
		<Footer />
	</body>
</html>

<style>
	#dev_guide {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: auto;
		transform: translate3d(0rem, 0rem, 0);
		opacity: 0.5;
		pointer-events: none;
		z-index: 9999999999;
	}
</style>

<script>
import { isSp_set } from "../utils/isSp";
import { isDev } from "../utils/isDev";
import { checkLocal } from "../utils/checkLocal";
import { pageTransition_set } from "../utils/pageTransition";
import { slTexts_init } from "../scripts/slTexts";
import { cta_init } from "../scripts/cta";
import { accordion_init } from "../utils/accordion";
import { pageTop_init } from "../scripts/page.top";
import { showImg_init } from "../scripts/showImg";
import { lenis_init } from "../libs/lenis";
import { toggleBgSection_init } from "../scripts/toggleBgSection";
import { header_init } from "../scripts/header";

window.addEventListener('DOMContentLoaded', () => {
	!isDev && checkLocal();
	setTimeout(() => {
		const pageId = document.getElementById('pageContainer')?.getAttribute('data-pageId') || ""
		function page_beforeEnter(resolve: (value: unknown) => void) {
			isSp_set();
			pageTransition_set();
			// lenis_init();
			slTexts_init();
			cta_init();
			accordion_init();
			showImg_init();
			toggleBgSection_init();
			header_init();

			switch (pageId) {
				case "top":
					pageTop_init();
					break;
				default:
					break;
			}
			resolve(null);
		}
		function page_enter() {
			switch (pageId) {
				case "top":
					break;
				default:
					break;
			}
		}
		Promise.resolve().then(() => new Promise(page_beforeEnter).then(page_enter));
	},)
})
</script>